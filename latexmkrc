# -*- perl -*-
# Set in xelatex mode, this is what the -xelatex option does.
$pdf_mode = 5; $dvi_mode = $postscript_mode = 0;
# By default just do the English version.
@default_files = ("unplugged-en");
